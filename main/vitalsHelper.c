#define startingOffset 6

int IDTovitalsIndex(int nodeID){
    int baseID= nodeID-startingOffset;
    //loop over excluded

    return baseID;
} //returns which index of vitalsArray a node corresponds to
